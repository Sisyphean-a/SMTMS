<Window x:Class="SMTMS.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMTMS.UI"
        mc:Ignorable="d"
        Title="{Binding ApplicationTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Scan Mods" Command="{Binding LoadModsCommand}" Margin="0,0,10,0" Padding="10,5" ToolTip="Reloads the list of mods from the directory."/>
                <TextBox Text="{Binding ModsDirectory}" Width="400" VerticalContentAlignment="Center" ToolTip="Path to the Mods directory."/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Import Legacy JSON" Command="{Binding ImportLegacyDataCommand}" Margin="0,0,10,0" Padding="10,5" Background="#FF9800" Foreground="White" BorderThickness="0" ToolTip="Import translations from old xlgChineseBack.json backup."/>
                <Button Content="Apply All Translations" Command="{Binding ApplyTranslationsCommand}" Margin="0,0,10,0" Padding="10,5" Background="#2196F3" Foreground="White" BorderThickness="0" ToolTip="Write all translations to manifest.json files."/>
            </StackPanel>
            
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="Mods">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Mods}" SelectedItem="{Binding SelectedMod}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="1" BorderBrush="LightGray">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150"/>
                            <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding UniqueID}" Width="200"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

                    <Border Grid.Column="2" BorderBrush="LightGray" BorderThickness="1" CornerRadius="3" Padding="15" Background="White">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMod}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Edit Mod Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                                <TextBlock Text="Name" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

                                <TextBlock Text="Author" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Author, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

                                <TextBlock Text="Version" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Version, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

                                <TextBlock Text="Unique ID" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.UniqueID, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5" IsReadOnly="True" Background="#F5F5F5" ToolTip="Mod ID cannot be changed safely."/>

                                <TextBlock Text="Description" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Description, UpdateSourceTrigger=PropertyChanged}" Height="120" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,5,0,15" Padding="5"/>

                                <Button Content="Save Changes" Command="{Binding SaveModCommand}" HorizontalAlignment="Right" Padding="20,8" Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    
                    <TextBlock Grid.Column="2" Text="Select a mod to edit details" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="14" FontStyle="Italic">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Hidden"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMod}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </TabItem>
            
            <TabItem Header="History">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Load History" Command="{Binding LoadHistoryCommand}" Margin="0,0,10,0" Padding="10,5" ToolTip="Loads the commit history."/>
                        <Button Content="Rollback to Selected" Command="{Binding GitRollbackCommand}" Margin="0,0,10,0" Padding="10,5" Background="#E74C3C" Foreground="White" BorderThickness="0" ToolTip="Reverts files to the state of the selected commit. WARNING: This is destructive!"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding CommitHistory}" SelectedItem="{Binding SelectedCommit}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="1" BorderBrush="LightGray">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Hash" Binding="{Binding ShortHash}" Width="80"/>
                            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
