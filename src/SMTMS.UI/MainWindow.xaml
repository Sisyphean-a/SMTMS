<Window x:Class="SMTMS.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMTMS.UI"
        mc:Ignorable="d"
        Title="{Binding ApplicationTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="扫描模组" Command="{Binding LoadModsCommand}" Margin="0,0,10,0" Padding="10,5" ToolTip="重新加载模组列表。"/>
                <TextBox Text="{Binding ModsDirectory}" Width="400" VerticalContentAlignment="Center" ToolTip="模组目录路径。"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="导入旧版数据" Command="{Binding ImportLegacyDataCommand}" Margin="0,0,10,0" Padding="10,5" Background="#FF9800" Foreground="White" BorderThickness="0" ToolTip="从旧版 xlgChineseBack.json 备份导入翻译。"/>
                <Button Content="应用所有翻译" Command="{Binding ApplyTranslationsCommand}" Margin="0,0,10,0" Padding="10,5" Background="#2196F3" Foreground="White" BorderThickness="0" ToolTip="将所有翻译写入 manifest.json 文件。"/>
                <Button Content="提取翻译备份" Command="{Binding ExtractTranslationsCommand}" Margin="0,0,10,0" Padding="10,5" Background="#9C27B0" Foreground="White" BorderThickness="0" ToolTip="提取翻译到 smtms_backup.json。"/>
                <Button Content="恢复翻译备份" Command="{Binding RestoreTranslationsCommand}" Margin="0,0,10,0" Padding="10,5" Background="#4CAF50" Foreground="White" BorderThickness="0" ToolTip="从 smtms_backup.json 恢复翻译。"/>
            </StackPanel>
            
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <TabControl Grid.Row="1" Margin="10">
            <TabItem Header="模组列表">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding Mods}" SelectedItem="{Binding SelectedMod}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="1" BorderBrush="LightGray">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="200"/>
                            <DataGridTextColumn Header="作者" Binding="{Binding Author}" Width="150"/>
                            <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="80"/>
                            <DataGridTextColumn Header="ID" Binding="{Binding UniqueID}" Width="200"/>
                            <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

                    <Border Grid.Column="2" BorderBrush="LightGray" BorderThickness="1" CornerRadius="3" Padding="15" Background="White">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMod}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Hidden"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="编辑模组详情" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                                <TextBlock Text="名称" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

                                <TextBlock Text="作者" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Author, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

                                <TextBlock Text="版本" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Version, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

                                <TextBlock Text="唯一ID" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.UniqueID, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5" IsReadOnly="True" Background="#F5F5F5" ToolTip="Mod ID cannot be changed safely."/>

                                <TextBlock Text="描述" FontWeight="Medium" Foreground="DimGray"/>
                                <TextBox Text="{Binding SelectedMod.Description, UpdateSourceTrigger=PropertyChanged}" Height="120" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,5,0,15" Padding="5"/>

                                <Button Content="保存更改" Command="{Binding SaveModCommand}" HorizontalAlignment="Right" Padding="20,8" Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                    
                    <TextBlock Grid.Column="2" Text="选择一个模组以编辑详情" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="14" FontStyle="Italic">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Hidden"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedMod}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </TabItem>
            
            <TabItem Header="历史记录">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="加载历史" Command="{Binding LoadHistoryCommand}" Margin="0,0,10,0" Padding="10,5" ToolTip="加载 Git 提交历史。"/>
                        <Button Content="回滚到选中" Command="{Binding GitRollbackCommand}" Margin="0,0,10,0" Padding="10,5" Background="#E74C3C" Foreground="White" BorderThickness="0" ToolTip="将文件恢复到选中提交的状态。警告：这是破坏性操作！"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding CommitHistory}" SelectedItem="{Binding SelectedCommit}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="1" BorderBrush="LightGray">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="哈希" Binding="{Binding ShortHash}" Width="80"/>
                            <DataGridTextColumn Header="信息" Binding="{Binding Message}" Width="*"/>
                            <DataGridTextColumn Header="作者" Binding="{Binding Author}" Width="150"/>
                            <DataGridTextColumn Header="日期" Binding="{Binding Date}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
