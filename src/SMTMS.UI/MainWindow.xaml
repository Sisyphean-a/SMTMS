<Window x:Class="SMTMS.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMTMS.UI"
        mc:Ignorable="d"
        Title="{Binding ApplicationTitle}" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Load Mods" Command="{Binding LoadModsCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <TextBox Text="{Binding ModsDirectory}" Width="400" VerticalContentAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Git Status" Command="{Binding GitStatusCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Git Pull" Command="{Binding GitPullCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <Button Content="Stage All" Command="{Binding GitStageCommand}" Margin="0,0,10,0" Padding="10,5"/>
                <TextBox Text="{Binding CommitMessage, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="0,0,10,0" VerticalContentAlignment="Center" /> 
                <Button Content="Commit" Command="{Binding GitCommitCommand}" Margin="0,0,10,0" Padding="10,5"/>
            </StackPanel>
            
            <TextBlock Text="{Binding GitStatusLog}" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Mods}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150"/>
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="80"/>
                <DataGridTextColumn Header="ID" Binding="{Binding UniqueID}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
